# ChildLens v1.2 native selective acquisition report

The frozen selected count was 15. The repository-safe receipt reports 15 acquired objects and 15 objects verified against the frozen selection. Acquisition completion is `true`.

Raw acquisition stayed below the frozen 20-GiB cap. Admission method: `CONSERVATIVE_DISPLAY_BOUND_WITH_HARD_COUNTER`. Transfer was sequential or tightly bounded: `true`.

No full archive, reselection, pre-transfer content inspection, duplicate full-resolution copy, external transfer, source name, restricted identifier, exact time, or restricted manifest was exported. Exact object hashes and linkage remain restricted; this report contains aggregate counts only.
