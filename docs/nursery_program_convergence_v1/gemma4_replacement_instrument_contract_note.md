# Gemma 4 replacement-instrument contract note

The immutable amendment [frozen_gemma4_replacement_instrument_amendment.json](frozen_gemma4_replacement_instrument_amendment.json) freezes a single prospective replacement for the failed Qwen2 visual path. It retains the existing Qwen3-VL hypotheses read-only and would compare them with one distinct Gemma 4 E4B local path. It does not revise or overwrite the earlier calibration protocols, retries, receipts, or decisions.

The replacement keeps exactly the same 15 primary items, 900 speech seconds, 137 ordered candidate windows, five frame offsets `[-5,-2.5,0,+2.5,+5]`, common categorical schema, K=5 complement protection, outward rounding, coverage/abstention thresholds, bootstrap plan, and no-majority-vote rule. Qwen3-VL is not rerun. Historical Qwen2 outputs remain preserved but are excluded from the replacement envelope.

For each frozen window, Gemma would receive the exact window audio plus the five ordered frames and one static prompt. The audio is decoded locally to mono 16-kHz PCM without normalization; the frame extractor is byte-for-procedure identical to the retained Qwen3-VL path. No transcript, translation, identifier, filename, confidence, previous prediction, or dynamic prompt text is supplied. Greedy decoding and a strict whole-output JSON parser enforce the existing five-field schema. One predeclared, path-wide correction may remove only an exact outer JSON markdown fence; it cannot alter prompts, models, windows, labels, definitions, gates, or thresholds and cannot be triggered by semantic disagreement or apparent quality.

The amendment is **not active**. Its revision, license-reconciliation, artifact-manifest, runtime, processor, tokenizer, template, adapter, and public-canary fields are explicit placeholders. A separate immutable activation receipt must replace every placeholder with exact verified values, reconcile the conversion's license metadata with its upstream, bind the amendment hash, and pass a self-generated joint audio/five-frame canary under network denial. Public access is not permission, and no agent may accept a click-through license.

Until activation passes, no restricted Gemma inference or Gemma-versus-Qwen3 comparison is permitted. After activation, outputs remain pseudo-labels inside owner-private quarantine; only K=5-protected, outward-rounded aggregates may leave. The ChildLens lexical STOP, simulator-oracle evaluation boundary, and no-outcome status remain unchanged.
