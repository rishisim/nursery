# Prototype-limited Gemma activation: technical revision disposition v1

## Decision

`PROTOTYPE_TECHNICAL_REVISE_PRE_OUTCOME`

The opaque-instrument provenance, cached-artifact, resource, runtime-overlay, privacy, and one-shot construction branches passed their public or synthetic checks. The fresh exact-common-schema canary did not reach model loading or generation. Its frozen public fixture failed while `/usr/bin/say` attempted to write little-endian PCM to the contract's AIFF intermediate. The current macOS `say` rejected that format/container pairing, so no canonical canary receipt, runtime lock, full activation receipt, restricted calibration receipt, pre-outcome receipt, authorization seal, or causal result was created.

This is a technical pre-outcome revision, not a null scientific result and not evidence about Gemma, Qwen3, ChildLens, language grounding, or the causal hypothesis.

## Why the existing correction was not used

The only frozen correction is one exact outer JSON-fence parser correction. The observed failure occurred before audio fixture construction completed and before any model output existed. Applying the parser correction would therefore be unrelated to the failure and would violate the frozen gate. No parser correction was spent.

## Public-only bounded remedy check

A diagnostic using only the same self-generated public German fixture confirmed that changing the *intermediate* container from AIFF to CAF lets `say` write the fixed 22.05-kHz little-endian PCM, after which the unchanged ffmpeg normalization produces the exact frozen final audio contract: mono, signed 16-bit PCM, 16 kHz, and 160,000 samples. This check did not modify the frozen canary, did not run Gemma, and did not access ChildLens.

The narrow next revision is therefore to freeze one additive, content-independent transport amendment that changes only `german.aiff` to `german.caf` in the public TTS intermediate and gives the revised failure/canary/activation receipts new immutable names. The final WAV, phrase, voice, rate, five frames, prompt, schema, parser allowance, artifact, 15-item/900-second/137-window sample, K=5 export policy, scientific thresholds, and causal protocol must remain byte-bound and unchanged.

## What remains ready

- The exact cached 5.18-GB Gemma artifact remains admitted for internal opaque-instrument use only; the disclosed converter-version, NOTICE, and conversion-metadata limitations remain unresolved and are not permission or quality evidence.
- The resource receipt still admits one MPS-heavy process, with a projected post-peak floor above 50 GiB.
- The Gemma/Qwen3 restricted coordinator remains fail-closed and has not opened the quarantine.
- The five-family symbolic runner remains construction-ready. Its public fixture produced identical deterministic merges at one and four workers, but no empirical calibration or scientific endpoint was opened.

The official source model is currently identified as Apache-2.0 by the [Google Gemma 4 E4B model card](https://huggingface.co/google/gemma-4-E4B-it) and Google's [Apache License 2.0 page](https://ai.google.dev/gemma/apache_2). This internal engineering disposition is not legal advice and does not cure the opaque conversion limitations.

## Michael-facing output status

No Michael-facing causal figure or finding was produced because the causal endpoint was never opened. Manufacturing a result panel from a failed fixture preflight would be misleading. The intended later central figure remains synchronized-minus-each-control noun/object and verb/action effects across the two instrument-specific ranges, their intersection, union, and conservative envelope.

> **Mandatory caveat box.** ChildLens remains a provisional local model-triangulation calibration anchor with no qualified German human gold. Model agreement is not ground truth or human reliability. Any later causal result is a symbolic simulator-oracle result, not evidence that ChildLens demonstrates acquisition, not an infant claim, and not a BabyView claim. No AEA or BabyView empirical artifact may enter this lineage.

## Exact next task

Freeze and sentinel-test the single CAF-intermediate transport amendment in a new immutable canary namespace. If and only if that fresh public canary and full activation pass, run the already prepared bounded Gemma calibration. Only a passed aggregate calibration and every unchanged pre-outcome falsification gate may authorize the existing one-shot causal command.
