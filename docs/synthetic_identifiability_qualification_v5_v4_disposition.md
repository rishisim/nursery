# V4 parent-audit disposition retained by v5

V4_READY is invalidated for promotion. All v4 files and claims are retained as evidence, and no v4 outcome is rerun.

The retained official frozen `test_execution_report.json` is `FAIL`, while the retained terminal records `tests=true`. The later corrected-environment report is `PASS`, but the frozen finalizer reads only the official report, so the recorded terminal cannot be regenerated from its official inputs.

The retained v4 no-sensor learner replaces every fitted manner prototype by `(0.5, 0.5)` only when the sensor is absent and evaluates with positional `argmax`. This makes its sensor-free gate candidate-order dependent rather than evidence of learned identification. The v4 factor “recomputation” invokes the same in-memory function twice in one process. Its prose traceability also names nonexistent `study.py` symbols and prose test labels that do not resolve to collected tests.

V4's validated detector invocation, balance, preservation, freeze, manifest, and corrected repository-environment test evidence remain useful package evidence. They do not cure the readiness defects or the scientific-design omissions: v4 has only synchronized/shuffled/absent conditions, always-present targets, and no matched shifts, deliberately uninformative control, or variable weak alignment.

