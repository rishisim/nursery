# Synthetic component-identification and non-floor robustness study v3

## Terminal recommendation: STOP

The single frozen development run does not support the synthetic sensor-added-value claim. Although the synchronized learner reached 100% on primary, seen, structured, component, and manner endpoints, every absent, shuffled, uninformative, and signed-shift primary action result was 0%. The two descriptive co-primary lifts were therefore identically +100 points in all 40 corpora. Under the frozen non-floor rule this is a design failure, not positive sensor evidence.

The attempted distribution-aware studentized-bootstrap interval failed because all 40 effects were identical: every bootstrap sample had zero standard error, leaving no finite studentized pivots. The frozen code was not amended and the outcome run was not repeated. Consequently there is no valid frozen co-primary confidence interval or test and the intersection-union claim cannot pass.

The direct failure diagnostics show a saturated synchronized solution: primitive and manner accuracy 100%, aggregate manner pairwise-order accuracy 100%, and zero collapse/inversion. Those diagnostics do not rescue the study because absent learning remained structurally ineffective. Matched nouns also rose from 0% absent to 100% synchronized, a severe selectivity failure.

All 2,880 expected run records are present with no duplicate cells: two separate regimes × 40 independent corpora × three stochastic model replicates × twelve learner/condition cells. The unbiased and adversarial `+3` regimes were never pooled. Eighty compressed raw visible/oracle corpus-regime ledgers occupy 92 MB. Fixture validation and the full repository suite passed before freeze (121 tests). The fixed v2 detector hash remained unchanged. Confirmation identifiers were blocked at every operation, and zero v1/v2/v3 confirmation outcomes were generated, read, evaluated, or summarized.

The result is limited to this synthetic feature-level weak lexical/action grounding design. It provides no infant, ecological, BabyView, ChildLens, raw-pixel/audio, general language acquisition, or unseen-word evidence. A future development version would need a demonstrably learnable sensor-free competitive baseline and an analysis that predefines behavior for degenerate bounded effects, but this v3 record does not authorize that work or any confirmation.
