# ChildLens calibration recovery v5

This is the privacy-safe public record for the separately versioned
“scale and lag-estimand correction” attempt.

The original execution stopped fail-closed at Stage 0 because inventory parsed
legacy mixed-scope manifests. That incident and its terminal records remain
immutable. The separately attested clean recovery then ran the unchanged
frozen method on exactly the 18 development participants while all 22 locked
participants remained sealed.

- `stage0_freeze_and_stop_receipt.json` binds the prospective protocol, code,
  public model revisions/hashes, immutable v1–v4 trees, and stop condition.
- `synthetic_positive_control.json` records the outcome-blind embedding-level
  code sensitivity check; it is not ChildLens evidence.
- `development_decision.{json,md}` records the terminal
  `NO_GO_UNINFORMATIVE`.
- `validation_receipt.json` validates the terminal record and privacy guard.
- `administrative_correction_receipt.json` and
  `clean_stage0_freeze_receipt.json` bind the exact-development-only repair
  and unchanged protocol.
- `clean_acquisition_receipt.json` records bounded selective acquisition and
  cleanup without exposing restricted bindings.
- `clean_model_hash_receipt.json` binds the two pinned frozen frontends.
- `clean_calibration_summary.json`, `clean_lag_response_report.json`, and
  `clean_development_decision.{json,md}` contain the privacy-safe clean-run
  results.
- `clean_validation_receipt.json` binds the restricted derivatives by hash and
  verifies zero locked access, cleanup, privacy, and terminal scope.

The exact v5 protocol is in
`docs/childlens_alignment_bridge_v5_protocol.md`. Its thresholds were not
changed during administrative recovery or after ChildLens outcomes. The clean
run ended `NO_GO_UNINFORMATIVE` because the frozen nuisance-matched support
gate failed; this is not evidence that true ChildLens grounding is absent.
